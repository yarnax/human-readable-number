module.exports = function toReadable (number) {
  let textNumber = number.toString();
  let answer = '';
  for (chr in textNumber) {
    switch (chr) {
      case "1":
        answer += ""
    }

  }
}
